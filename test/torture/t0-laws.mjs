/**
 * Tier T0 -- metamorphic laws (registered placeholder for O1-O3).
 *
 * O0 ships the pair lifecycle only; the metamorphic laws worth pinning here
 * (traversal-vs-oracle invariants, filter-vs-postfilter equality, swept-superset)
 * all belong to later sessions. Registered now to fix the tier order so a later
 * session fills a body without touching torture.mjs.
 */
export function run() {
    // intentionally empty -- filled by O1-O3.
}
